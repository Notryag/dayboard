[project]
name = "agent-application-platform"
version = "0.1.0"
description = "Reusable application capabilities for North-based conversational products."
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[tool.uv.build-backend]
module-name = "agent_platform"

[build-system]
requires = ["uv_build>=0.11.28,<0.12.0"]
build-backend = "uv_build"
